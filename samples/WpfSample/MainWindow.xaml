<Window x:Class="MediaEmbedKit.Mpv.Samples.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mpv="clr-namespace:MediaEmbedKit.Mpv.Wpf;assembly=MediaEmbedKit.Mpv.Wpf"
        Title="MediaEmbedKit.Mpv WPF Sample"
        Width="960"
        Height="540">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="48" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0"
              Margin="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="96" />
                <ColumnDefinition Width="96" />
                <ColumnDefinition Width="88" />
            </Grid.ColumnDefinitions>

            <TextBox x:Name="UrlTextBox"
                     Height="32"
                     Margin="0,0,8,0"
                     VerticalContentAlignment="Center"
                     Text="https://www.youtube.com/watch?v=dQw4w9WgXcQ" />
            <Button Grid.Column="1"
                    Width="88"
                    Height="32"
                    Margin="0,0,8,0"
                    Content="Load"
                    Click="LoadClick" />
            <Button Grid.Column="2"
                    Width="88"
                    Height="32"
                    Margin="0,0,8,0"
                    Content="Pause"
                    Click="PauseClick" />
            <Button Grid.Column="3"
                    Width="88"
                    Height="32"
                    Content="Stop"
                    Click="StopClick" />
        </Grid>

        <mpv:MpvWpfPlayer x:Name="PlayerHost"
                          Grid.Row="1">
            <mpv:MpvWpfPlayer.OverlayContent>
                <Grid IsHitTestVisible="False">
                    <Border Width="64"
                            Height="6"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Bottom"
                            Margin="12"
                            Background="#CCFFFFFF"
                            CornerRadius="2" />
                </Grid>
            </mpv:MpvWpfPlayer.OverlayContent>
        </mpv:MpvWpfPlayer>
    </Grid>
</Window>
