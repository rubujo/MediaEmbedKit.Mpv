# 共用 AI Agent 指南

此文件僅保留舊路徑相容用途。主要入口為根目錄 `AGENTS.md`。

## 使用順序

1. 閱讀 `AGENTS.md`。
2. 依任務閱讀 `docs/PROJECT_SPEC.md` 與專責文件。
3. 編修專案時使用 `docs/ai/skills/mediaembedkit-mpv.md`。
4. 追蹤 提供者 git build 或比對 libmpv header 時使用 `docs/ai/skills/libmpv-git-build-tracker.md`。

不得在本文件新增專案規則。
